{
  "name": "tempwether", "version": "1.0.0", "private": true, "type": "module",
  "description": "Israel and the eastern Mediterranean weather observatory",
  "scripts": { "dev": "node scripts/serve.mjs", "start": "node scripts/serve.mjs", "build": "node scripts/build.mjs", "test": "node --test tests/*.test.mjs", "collect": "node scripts/collect.mjs" },
  "engines": { "node": ">=22" }
}
